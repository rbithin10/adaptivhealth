/* setupTests.ts — Prepares the testing environment before any test runs */

// Load extra test helpers that let us check things like "is this button visible on the page?"
import '@testing-library/jest-dom';
