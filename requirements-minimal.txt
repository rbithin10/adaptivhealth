# Minimal requirements to run the backend
# Install with: pip install -r requirements-minimal.txt

# Web Framework (core)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# Database (SQLite is built-in, only need SQLAlchemy)
sqlalchemy>=2.0.30

# Data Validation
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1.1

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.3

# Encryption
cryptography>=44.0

# Env
python-dotenv>=1.0.1

# ML (required for predictions)
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
